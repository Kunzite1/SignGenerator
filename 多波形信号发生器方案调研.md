# STM32 多波形信号发生器课设方案与开源项目调研

> 调研日期：2026-09-05。目标是用尽量少的模块，在 150 元预算内完成报告、示波器测量和演示视频；不追求产品级精度、带载能力或长期可靠性。

## 1. 结论：推荐直接用片内 DAC

主方案选 **STM32F103RCT6（或 RET6）最小系统板 + 0.96 英寸 I²C OLED + EC11 旋转编码器**。PA4 的片内 12 位 DAC 经 1 kΩ 串联电阻直接接高阻示波器，先不加外置 DAC、运放和滤波器。

选择它而不是常见的 STM32F103C8T6，是因为 C8 没有片内 DAC，必须另接 R-2R 电阻网络或串行 DAC；RCT6 则集成两个 12 位 DAC、ADC、DMA 和定时器，接线更少。ST 的[产品页](https://www.st.com/en/microcontrollers-microprocessors/stm32f103rc.html)、[数据手册](https://www.st.com/resource/en/datasheet/stm32f103rc.pdf)和[DAC 波形生成应用笔记 AN3126](https://www.st.com/resource/en/application_note/an3126-audio-and-waveform-generation-using-the-dac-in-stm32-products-stmicroelectronics.pdf)可作为报告中的原理依据。

建议把必做范围定为 **四种波形均可在 1 Hz～10 kHz 调节**。这对课设演示足够，也给 DAC 更新率和面包板接线留出余量。

## 2. 与任务书要求的对应关系

| 要求 | 最小实现 |
|---|---|
| 正弦、方波、三角波、锯齿波 | 查表生成，TIM6 触发 DAC，DMA 循环搬运 |
| 外部切换波形、调频 | EC11 旋转调频，按下切换波形；另加一键启停 |
| 显示波形和频率 | SSD1306 128×64 OLED 显示类型、设定/实际频率和步进 |
| 计算上下限和最小步进 | 根据定时器公式计算，并与示波器读数做误差表 |
| 白噪声 | DAC 自带 LFSR 噪声模式，或循环播放伪随机数组 |
| AM、FM | 用固定载波/调制参数预生成数组，DMA 循环输出 |
| 个性化功能 | 做自动扫频，比再加硬件更省时且视频效果直观 |

## 3. 硬件连接

```text
EC11/按键 ──> STM32F103RCT6 ──I2C──> OLED
                    │
              TIM6→DMA→DAC1
                    │ PA4
                  1 kΩ ──> OUT/BNC ──> 示波器（1 MΩ或×10探头）
```

推荐引脚如下；购买前仍需核对所选最小板是否引出了 PA4、PB6、PB7。

| 功能 | 引脚 | 说明 |
|---|---|---|
| 模拟输出 | PA4 / DAC_OUT1 | DAC 输出缓冲使能，串联约 1 kΩ |
| OLED | PB6/PB7 | I2C1 SCL/SDA，模块用 3.3 V 供电 |
| EC11 A/B | PA6/PA7 | TIM3 编码器模式，或普通 GPIO 扫描 |
| EC11 按键/启停键 | PB0/PB1 | 内部上拉，按键对地 |
| 可选电位器 | PA0 / ADC1_IN0 | 10 kΩ电位器调幅，体现 ADC 功能 |
| 调试串口 | PA9/PA10 | 可选，用于打印或 VOFA+ 观察参数 |
| 下载调试 | PA13/PA14 | SWDIO/SWCLK，保留复位和公共地 |

所有模块必须共地。DAC 输出是约 0～3.3 V 的单极性信号，可令中点为 2048、幅值约 1600～1700 个码，避免贴近电源轨。若老师想看“以 0 V 为中心”的图，可把示波器设为 AC 耦合并在报告中注明；没有双电源要求时，不必为负电压输出增加复杂模拟电路。

## 4. 器件选型与预算

以下是采购预留值，不是实时报价；下单当天再核价。

| 器件 | 推荐规格 | 数量 | 预算/元 | 选择理由 |
|---|---|---:|---:|---|
| MCU 最小系统板 | STM32F103RCT6/RET6，PA4 已引出 | 1 | 25～35 | 片内 DAC/ADC/DMA，必需功能一板完成 |
| 下载器 | ST-Link V2 兼容版 | 1 | 10～20 | SWD 下载和调试；已有则不买 |
| 显示模块 | 0.96 英寸 SSD1306，128×64，4 针 I²C | 1 | 8～15 | 接线只需四根，例程多 |
| 输入模块 | EC11 编码器模块，带按键 | 1 | 3～8 | 同时完成调频、选项和确认 |
| 可选模拟输入 | B10K 线性电位器模块 | 1 | 2～5 | 调幅或演示 ADC，不是必需 |
| 搭建材料 | 面包板、杜邦线、按键、排针 | 1 批 | 15～25 | 先快速验证，不急着画 PCB |
| 输出与保护 | 1 kΩ、BNC 接线柱或 SMA 转接线 | 1 批 | 5～15 | 方便接示波器并限制误接电流 |
| 可选转接板 | 嘉立创 2 层小板 | 1 批 | 20～25 | 固定模块和接口，基本功能通后再下单 |

不含可选电位器的面包板版预算为 **66～118 元**；加入电位器和转接板后按最高值约 **148 元**。首轮不要购买 AD9833 或运放；只有 PA4 直连示波器确实失真时，再加 3.3 V 供电的轨到轨跟随器模块（如 MCP6002/LMV358）。

可直接使用的采购搜索词是“`STM32F103RCT6 核心板 PA4`”“`SSD1306 0.96 OLED IIC 4针`”“`EC11 旋转编码器模块`”和“`BNC 母座转接板`”。核心板应确认芯片尾缀是 **RCT6/RET6**、PA4 和 SWD 均已引出，排针最好已焊；不要仅凭商品标题中的“F103”下单。

若画转接板，只画“底板”：放最小板母座、OLED/EC11/电位器接口、SWD、输出接口和少量阻容，不重画 MCU 核心电路。PA4 到输出接口走线尽量短，模拟输出附近留测试点。

## 5. 固件实现建议

推荐 STM32CubeIDE + HAL；若团队更熟悉 Keil，也可由 CubeMX 生成 Keil 工程，但不要在同一工程混用标准库与 HAL。OLED 可参考 MIT 许可的 [stm32-ssd1306](https://github.com/afiskon/stm32-ssd1306)。

关键配置：系统时钟 72 MHz；TIM6 更新事件作为 TRGO；DAC1 由 TIM6 触发、输出缓冲开启；DMA 设为半字、存储器递增、循环模式；I2C1 驱动 OLED；TIM3 读取 EC11。

基本波形都生成 0～4095 的无符号数组：

- 正弦：`mid + A·sin(2πn/N)`；
- 方波：前半周期 `mid+A`，后半周期 `mid-A`；
- 三角波：按周期位置线性上升、下降；
- 锯齿波：`mid-A + 2A·n/(N-1)`。

同一档内改变频率时只更新 TIM6 的 PSC/ARR，不必重新生成基本波形。为兼顾波形平滑和高频上限，可预存三组表：1～1000 Hz 用 `N=256`，1～5 kHz 用 `N=128`，5～10 kHz 用 `N=64`；跨档时切换 DMA 数组和长度。每次都重新计算实际频率并显示，避免屏幕只显示理想设定值。

### 频率上下限与步进

设定时器时钟为 `f_TIM`，则：

```text
f_sample = f_TIM / ((PSC + 1)(ARR + 1))
f_out    = f_TIM / ((PSC + 1)(ARR + 1)N)
ARR      = round(f_TIM / ((PSC + 1)N f_set)) - 1
```

最后的 `-1` 不能漏掉。以 `f_TIM=72 MHz` 为例：

- 16 位 PSC、ARR 且 `N=256` 时，纯定时器理论最低约 `6.55×10⁻⁵ Hz`；实际菜单下限定为 1 Hz，避免一次测量等数小时。
- 参考数据手册给 DAC 留出不高于约 1 MSa/s 的更新率，则 `N=256` 时约 3.91 kHz、`N=64` 时约 15.63 kHz；因此把可验收上限定为 10 kHz。
- 1 Hz、`PSC=4`、`N=256` 时，`ARR=56249`，相邻 ARR 对应的局部理论步进约 `1.78×10⁻⁵ Hz`。
- 10 kHz 附近、`PSC=0`、`N=64` 时，相邻设置约为 9955.8 Hz 和 10044.6 Hz，硬件量化间隔约 88.9 Hz。

“旋钮每格 1/10/100 Hz”是人为设置的 UI 步进，不等于定时器的最小可实现步进；报告应分别说明。四种基本波形共用相同采样链路，因此理论频率公式相同，但高频方波/锯齿波会因 DAC 建立时间、面包板和探头而更早出现边沿失真。

### 白噪声、AM、FM 与扫频

- 白噪声：最省代码的方法是使用 STM32 DAC 的伪随机噪声发生器；AN3126 给出了噪声和三角波模式说明。
- AM：固定 `fs=100 kSa/s、fc=1 kHz、fm=50 Hz、m=0.7`，预生成
  `2048 + A[1+m·sin(2πfm n/fs)]sin(2πfc n/fs)`。
- FM：固定相同载波和调制频率，预生成
  `2048 + A·sin(2πfc n/fs + β·sin(2πfm n/fs))`，可取 `β=5`。
- 扫频：每 20 ms 调整一次 TIM6，让频率在 100 Hz～5 kHz 往返；它适合作为个性化功能，不需要新硬件。

长度 2000 的 `uint16_t` 调制表只占约 4 KiB。课设只需固定参数能在示波器上看出包络或疏密变化，无须实现实验室级实时调制器。

## 6. 可参考的开源项目

没有检索到一个“直接复制即可同时满足四种基本波形、显示、噪声、AM、FM”的完整项目。下列项目适合分模块借鉴；使用代码前要查看对应许可证，未给许可证的项目只参考思路。

| 平台与项目 | 可借鉴内容 | 不宜照搬之处 |
|---|---|---|
| 立创开源硬件：[F103RCT6 波形发生器](https://oshwhub.com/Fangbrbr/stm32-kai-fa-ban) | 与主方案同芯片；PA4 DAC、四种波形、OLED/按键、工程与 PCB 附件，最贴近本题 | 旧库工程风格；频率范围和计算需自行实测复核 |
| Gitee：[SimpleSignalGenerator_BaseOnStm32HAL](https://gitee.com/chenjjian/SimpleSignalGenerator_BaseOnStm32HAL) | G431 + DAC/TIM/DMA/片内运放，四波形、按键、串口、LCD，HAL 分层清楚 | 仓库中的 F103ZE 分支未充分验证；移植时按上式复核 ARR 的 `-1` |
| GitHub：[BluePillWaveGen](https://github.com/BojanSof/BluePillWaveGen) | F103C8 + 8 位 R-2R DAC、OLED、EC11、CubeIDE、KiCad 和实测图，UI 很值得参考 | 接线和模拟级较多；根目录未见明确许可证，不要直接复制发布代码 |
| Gitee：[course-design](https://gitee.com/Yuboling/course-design) | F103C8 + TLC5615 + ST7735 + 按键，已有正弦/方波/三角波/噪声 | 串行 DAC 和忙等待限制速度；还需把噪声表改成锯齿表 |
| GitHub：[stm32_functgen](https://github.com/angst7/stm32_functgen) | STM32F4 片内 DAC、DMA 双缓冲和四波形，适合学习数据流 | 没有本地显示；使用 F4，不是直接的 F103 工程 |
| GitHub：[STM32F103C8T6OLEDOscilloscope](https://github.com/siliconvalley4066/STM32F103C8T6OLEDOscilloscope) | OLED/五向键 UI；DDS 表中含三角、锯齿、方波及噪声 | PB9 PWM+RC 的“DAC”只适合较低频和低成本演示 |
| 立创开源硬件：[F407 DAC 波形部分](https://oshwhub.com/zk272761180/novice-to-sky-star-digital-oscil) | DAC+TIM+DMA、EC11、正反锯齿等算法 | 原项目是四层板示波器，整机严重超出课设范围 |
| GitHub：[Function-Generator](https://github.com/MUDAL/Function-Generator) | AD9833、1602、按键、幅值/偏置模拟级，资料较完整 | AD9833 原生只有正弦/三角/方波，没有锯齿和白噪声 |

如果后来确实需要远高于 10 kHz 的正弦波，可将 [AD9833 模块开源硬件](https://oshwhub.com/eda_rgfawoazf/dian-sai-mo-kuai-ad9833-ke-bian-cheng-bo-xing-fa-sheng-qi-mo-kuai)作为扩展。ADI 的[官方产品页](https://www.analog.com/en/products/ad9833.html)也明确列出的原生波形只有正弦、三角和方波，所以它不应作为本题唯一波形源。

## 7. 备选路线

1. **STM32G431RBT6 最小板**：性能余量最大，片内有 DAC 和运放；ST 还提供了 [NUCLEO-G431RB 的 DAC+DMA+OPAMP 官方示例](https://github.com/STMicroelectronics/STM32CubeG4/blob/master/Projects/NUCLEO-G431RB/Examples/OPAMP/OPAMP_PGA/readme.txt)。若能方便买到板且不介意换芯片，这是最好的性能备选。
2. **已有 F103C8T6 时继续使用**：加 TLC5615 模块最省焊接，或照 BluePillWaveGen 做 8 位 R-2R；缺点是连线更多、波形品质和调试工作量更难预测。
3. **F103RCT6 + AD9833 双波形源**：只在老师强求 MHz 级正弦时考虑。四种基本波形仍由 MCU DAC 输出，AD9833 单独提供高频正弦；需要输出切换，报告和接线都会变复杂。

## 8. 两周内的实施与验收顺序

1. 第 1～2 天：面包板接 OLED、EC11、PA4 输出，只做 1 kHz 正弦并用示波器确认。
2. 第 3～5 天：完成四张表、DMA 循环、频率分档和实际频率显示。
3. 第 6～7 天：测四种波形的低/中/高频点，形成“理论值—实测值—误差—现象解释”表。
4. 第 8～10 天：加入噪声、固定 AM/FM 和扫频；基本功能没稳定前不要做高级功能。
5. 第 11 天后：若确有需要再画转接板；否则整理接线、录视频、完成报告。

视频按“开机界面→四波形切换→旋钮调频→上下限/步进→白噪声→AM/FM→自动扫频”的顺序录制，并让 OLED 和示波器读数同框。报告应包含系统框图、接线/原理图、CubeMX 配置、波形表与 DMA 流程、频率公式、测量表、示波器截图、误差原因和改进方向。

最重要的止损线是：**四种基本波形、旋钮、OLED 和测量表完成后就冻结硬件**。白噪声、AM、FM、扫频尽量只改软件，避免后期重做模拟电路。

## 9. 已知局限与可写入报告的改进方向

- 片内 DAC 只能输出单极性、小功率信号，不能直接带低阻负载；产品化时可增加带增益、偏置和保护的输出级。
- 方波和锯齿波在高频处会出现阶梯、圆角和过冲；可提高采样率、使用外置高速 DAC，并给正弦输出增加可切换重建滤波器。
- 频率由整数 PSC/ARR 量化，显示值与设定值不完全相同；可自动搜索最优 PSC/ARR，或用相位累加 DDS 获得更细步进。
- 面包板连线会引入寄生参数；若测试重复性不佳，再把已验证的接口固化到转接板，并缩短模拟地回路。
- 当前 AM/FM 采用固定参数表；后续可做双缓冲实时运算，让编码器分别调载波频率、调制频率和调制度。
